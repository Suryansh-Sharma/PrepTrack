spring:
  application:
    name: preptrack
  datasource:
    url: jdbc:postgresql://localhost:5432/preptrack
    username: postgres
    password: Suryansh@1904
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

mailtrap:
  token: ${MAILTRAP_API_TOKEN}
  from-email: ${MAILTRAP_FROM_EMAIL}
  from-name: ${MAILTRAP_FROM_NAME:PrepTrack}

app:
  frontend-url: ${APP_FRONTEND_URL:http://localhost:4200}
  verification-url: ${APP_VERIFICATION_URL:http://localhost:4200/verify-email}
  forgotpassword-url: ${APP_VERIFICATION_URL:http://localhost:4200/forgot-password}

security:
  jwt:
    secret-key: 3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
    expiration-time: 3600000
    refresh-token-day: 30
    email-verification-time: 15m
logging:
  level:
    org.hibernate.SQL: DEBUG
server:
  servlet:
    context-path: /api/v1
resend-cooldown-second: 60